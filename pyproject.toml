[project]
name = "aivideodetector"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "torchvision",
    "numpy",
    "pandas",
    "pyarrow",
    "wandb",
    "huggingface_hub",
    "av",
    "tqdm",
    "requests",
    "Pillow",
    "scipy",
    "scikit-learn",
]

[project.optional-dependencies]
gen = [
    "fal-client",
    "Pillow",
]
gen-local = [
    "diffusers",
    "transformers",
    "accelerate",
    "safetensors",
    "sentencepiece",
    "protobuf",
]

[tool.uv]
package = false
